<div class="container">
<div class="jumbotron">
  <h1>Tags</h1>
 </div>
   


<% @tags.each do |f| %>
<div class="col-sm-6 col-md-3">
<div class="thumbnail">
<% if f.photos.present? %>
<td><%= link_to image_tag(f.photos.first.image.url(:medium)), '#', class: "thumbnail" %> </td>


<% else %>
<%#debugger%>
<img src="http://i.imgur.com/vGsqNpo.jpg"), class="thumbnail"></img>
<% end %>
 <div class="caption">
<h3 ><a href="<%= tag_path(f.id) %>" class="btn btn-warning btn-lg pull-right"><%= f.tname %>    <span class="badge"><%= f.photos.count %></span></a><br></h3>
</div>
</div>
</div>
<%end%>
</div>