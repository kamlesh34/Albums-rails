<!DOCTYPE html>
<html>
<head>
  <title>FileTrans</title>
   <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>

  <nav class="navbar navbar-default">
  <div class="container">
  <div class="row">
 
  <% if current_user %>
 <div class="navbar-header">

      <a class="navbar-brand navbar-text navbar-left" href="#">
      	Welcome <%= current_user.username %>      	
      </a>
      </div>

<div class="collapse navbar-collapse">
<ul class="nav navbar-nav">
<% if params[:action]=="index" %>
  <% if params[:controller]=="albums"%>
<li class="active"><%= link_to "All Albums", user_albums_path(current_user.id) %></li>
<li ><%= link_to "Tags", tags_path %></li>
  <%else%>
    <li><%= link_to "All Albums", user_albums_path(current_user.id) %></li>
<li class="active"><%= link_to "Tags", tags_path %></li>  
  <%end%>
  <%else%>
  <li><%= link_to "All Albums", user_albums_path(current_user.id) %></li>
<li ><%= link_to "Tags", tags_path %></li>
<%end%>
</ul>
<ul class="nav navbar-nav navbar-right">

<li class="dropdown">
<a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Signed in as <%= current_user.email %> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li>
              <%= link_to destroy_user_session_path, :method => :delete, class: "btn-default"  do %>
<i class="glyphicon glyphicon-off"></i> Logout
<%end%>
            </li>

      <li role="separator" class="divider"></li>
      <li><a href="#">About</a></li>
</li>

</ul>
</div>
</div>
 <% else %>
<div class="navbar-header">
      <a class="navbar-brand navbar-text navbar-left" href="#">
      	Login/Signup      	
      </a>
      </div>
  <%end%>
</head>
<body>

<%= yield %>

</body>
</html>
